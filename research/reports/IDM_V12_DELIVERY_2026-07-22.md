# IDM v12 Follower 交付记录（2026-07-22，r2 附对抗审查结论）

> **r2**：对抗性统计审查（IDM_V12_ADVERSARIAL_REVIEW）判定 +0.093R 大概率为选择噪声+单笔彩票，成本后
> 前向点估计 ≈ −0.15R/笔；v12 跟单模块定位改为**预登记试验品**（淘汰制前向协议见审查报告 §6b），
> 不是已验证的正期望系统。当日晚间另修复：CE10057（状态机与效果分离重构）、3m/10m 双窗一致性
> （跟单状态镜像上 10m 窗，TV v13.0，SHA `019f23d3`）、面板品牌 IDM v12。

用户指令：结合 Ripster/Saty 原版工具重做；解释并根治 S1/R1 跳变；根治"卖A卖A卖A一路涨"；
找交易/指标专家 review。冻结 `11.0.0-clean` 依旧只读（SHA `77c6fb40…` 复验通过）。

## 三条病根的处置

1. **S1/R1 跳变**：引擎 S1/R1 是六源混合"最近合格位"（Saty 梯位、摆动点、昨高低、
   3m 5/12 云边、3m 34/50、10m 34/50，容差 0.10×ATR）。其中三源是**每根 K 都动的均线云边**，
   价格骑云时 S1/R1 贴脸且逐 bar 跳——这不是 bug，是把"动态云边"当"支撑压力"报出来的设计错误。
   处置：v11.2 已撤下图；v11.2.3 把面板读数也换成 Saty 式稳定读数（日锚 + 已走%日ATR，
   与用户左窗真 Saty 表逐值一致）；引擎内部照旧用它触发（冻结不动），仅不再展示。
2. **卖A 连发**：引擎允许重叠计划/同向再入场，回调行情里反复进场反复止损。
   处置：v12 跟单模块 = 单仓（持仓期忽略新信号）+ 全额止损后同向冷却 30 分钟（可调）。
3. **Cloud 不像 Ripster**：按研究报告原版规范重做云层——5/12 高饱和绿/红动量云（transp 76）、
   3m 34/50 结构云用 Ripster 原版 Trend Cloud 色 **#2196f3 蓝 / #ffb74d 浅橙**（transp 82）、
   确认 10m 34/50 同色系更淡作背景带；线弱化、以色带为主。engine EMA 用 close（冻结），
   与原版 hl2 有细微差异，已记录。

## v12 跟单模块（12.0.0-follower）

实验室定案规则（证据见 IDM_V12_EXIT_LAB）：只跟关键位拒绝；默认早午盘 09:30–14:00 ET；
单仓；止损**全程不动**；T1 兑现 50%、T2 兑现 25%、余 25% 日终或止损；全额止损后同向冷却；
逆势拒绝默认跟。图上：开仓"跟多/跟空"标签、平仓标签带本笔净 R、四条计划线（止损红实线）、
面板"计划"行=跟单状态+累计 R 记分板（注意：累计值随图表加载的历史深度变化，非正式回测）。
推送：【v12跟单】开/平/T1/T2 全事件；旧版每信号推送与旧计划事件推送默认关闭
（输入项可恢复）；Saty 二拒推送保留。引擎/计划/订单逻辑零改动（契约测试守护：
模块内无 strategy.*、`f12.stop :=` 恰好一次）。

同时清理：删 5 个纯诊断 plot（TV 64-plot 硬上限，v11.2.3 加结构云后 67 超限）。

## TradingView 部署

- 脚本 `USER;213fe81c…`：v8.0（67 plots 编译失败）→ v9.0（v11.2.3，SHA `5754473d`）→
  v10.0（v12.0.0，SHA `8443e514`）→ **v11.0（+Ripster 34/50 配色，SHA `eef9dd58` 与本地逐字节一致，编译通过）**。
- 标题改为 "IDM v12 Follower"（图例 IDM v12）；库内名仍 "IDM v11.1 Clear"（历史遗留，仅显示）。
- **提醒重建**：旧 "IDM v11.2" alert 已删（TV alert 绑定创建时版本，不随保存更新）；
  新 alert：IDM v12 · SPX500 3m · alert() calls only · App push · 到期 2026-08-22。DS Bridge ×2 未动。
- 布局插曲：两次误触把代码贴成图上文字批注（text drawing），均已删除（getAllShapes/removeEntity）。

## 测试

`91 passed / 4 skipped`（新增 test_v12_follower_module_contract）。冻结源 SHA 不变。仓库未 commit。

## 边界与下一步

r2：见对抗审查——收缩+成本后点估计为负，前向 OOS 按淘汰制协议执行（记分板只进不改，改规则=清零）。
v12.1 候选（按研究报告）：10m 云偏置闸门+云内禁发、Range≥90% 耗尽闸门、入场区带不追价、
滞回双阈值、趋势日例外；引擎级重构继续遵守一次一项+OOS。
