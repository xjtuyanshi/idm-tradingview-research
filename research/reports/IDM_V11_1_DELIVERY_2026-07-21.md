# IDM v11.1 Clear 交付记录（2026-07-21 晚）

冻结 `11.0.0-clean` 保持只读（SHA 复验通过）。本次交付新版本文件 `intraday_decision_map_v11_1_clear.pine`（1781 行，`11.1.0-clear`）。

## 交付内容

1. **引擎零漂移**：v11.1 的信号/计划/订单引擎区从冻结源逐字节复制；`test_v11_1_clear_contract.py` 断言冻结引擎全文逐字包含于新文件。上图实测：与冻结基线重叠窗口信号 id 185/186 一致（1 例为已记录的亚 1e-11 浮点边界类，见 parity 报告 §4）。
2. **Saty 二次拒绝 AdvisoryEvent**（P2 实验按既定契约）：双侧状态机 IDLE→WATCH→DEPARTED→fire；档位身份 =（锚定日, 比例）；离开距离参数 0.05×日 ATR；确认收盘穿位/换锚即清零；`Saty二拒↑/↓` 标签 + 全脉络 tooltip + 自然中文推送；10m 经扩展至 20 元素的 sparse pulse 元组同源 relay。静态测试断言模块内零 `strategy.*`、零计划变量触碰；开关关闭时对冻结行为零扰动。
3. **图面分层降噪**（可视化整改，全部在引擎之外）：全尺寸「买/卖 A-B-C」只留顺势开仓与反手；逆势试仓（研究窗口占 35%）降级为紫色小标「逆多/逆空」；加仓参考（占 20%）降级为小点且默认隐藏；云层与 S1/R1 透明度整体调淡；新增两个显示开关。净效果：默认视图全尺寸标签约减少三分之二。
4. **数据窗口新增**：Saty advisory id/level/side（脉冲式）、双侧 watch 状态，供后续 advisory 账本对账。

## TradingView 部署与验证

- 已保存至用户脚本库：`IDM v11.1 Clear`（scriptId `USER;213fe81c…`）。首次保存（v1.0）经剪贴板粘贴时中文被 MacRoman 双重编码破坏（`cat | pbcopy` 无 LANG 之坑；本地 pbpaste 校验因编码对称回转而漏检）；以 `LANG=en_US.UTF-8` 重灌剪贴板后重贴并另存 **v2.0**：云端规范化 SHA 与本地文件完全一致（`1c56a060…`），`现在/买/Saty二拒` 等中文全部完好。
- v2.0 编译通过（Pine v6），SPX500 3m 挂图运行 `failed:false`；订单默认关闭（Strategy Tester 显示无交易数据 ✓）。
- 10m relay 主机（v1.0 阶段实测）：212 个 relay 信号中 211 个与 3m 账本同 id 同元数据；advisory relay 通道与标签/推送去重游标全部就位。
- 脚本已加载到用户 `daytrade_codex_trest` 布局（SPX500 窗格）。
- 遗留：交付当晚 TradingView 对本会话的历史行情通道出现持续限流（任意周期只回 2 根 K，live tick 正常）——属服务端节流，与脚本无关；恢复后打开图表即可见完整渲染。

## 用户手册

网页版详细手册（中文、含图解标记词典/规则/FAQ/诚实限制）已发布：
`https://claude.ai/code/artifact/856d1f1d-bc82-4fa4-9377-904fe5f44ed4`

## 测试基线

`83 passed / 4 skipped`（含新增 8 项 v11.1 契约测试与此前的 4 项真 fixture parity 守护）。冻结源 SHA 不变。

## 附录：独立双 agent 审计结论（2026-07-22）

两个独立 review agent 分别审计了 v11.2 Pine 全文与统计代码，全部发现已分级处置：

**统计代码审计（1 项重大 + 2 项中低）**：T2 全额了结漏算（重大，已修复并全量重跑，初版数字作废，见 r2 报告）；MFE/MAE 含出场 K（已修复）；结果标签死代码（已修复）。

**Pine 审计（8 项）**：
- 已修复：标签/线条 GC 会杀死 10m 主机上的 var 单例 UI（改为每次渲染重建 + max_lines 提头寸）；最新信号卡片无视显示过滤（已按 markerPrimary 门控）；加仓小点缺启动过滤（已补）；提醒过滤函数定义序（已修）。
- 冻结架构固有、记录为 v12 事项：**3m 不整除 10m 导致 security_lower_tf 中继在实时 10m 图上可能丢/迟约 20% 的脉冲**（手机提醒建在 3m 图上不受影响；v12 候选：中继改 9m/12m 或按收盘时间重装箱）；10m 主机历史 K 的稠密状态显示含最多 2 分钟的"未来"3m 状态（仅显示层，事件不受影响）；advisory 的 varip 竞态与引擎同款（罕见 tick 时序）。
- 记录为使用注意：计划事件/Saty 提醒 24 小时推送不受 06 组过滤（持仓管理设计使然，孤儿场景已写进手册）。

TV 脚本经 v5.0（编译错）→ v6.0（运行时错）→ **v7.0（通过，SHA `969c3eed` 与本地逐字节一致，3m/10m 双主机 failed:false）**。
