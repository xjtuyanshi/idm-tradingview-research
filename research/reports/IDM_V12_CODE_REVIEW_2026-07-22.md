# IDM v12 代码审查（独立 agent，2026-07-22）——发现与处置记录

> 只读审查 2256 行（12.0.0-follower）。全部发现已在 **12.0.1-follower**（TV v14.0）处置完毕。
> 结构地图已整理入 `docs/ARCHITECTURE_V12.md`。

## 审查确认的边界事实

- 字节冻结区实际为 `f_v11_engine` → 分隔线（约 L442–940）；**Saty 二拒段与 f_alert_pass 不在字节冻结区**，属行为契约区（`f_alert_pass(`×3 由测试钉住）。
- **冻结洞（重要）**：引擎喂给函数（f_pick_*、f_candidate_*、f_nearest_saty、f_setup_priority）与引擎参数输入定义在冻结切片之外——改它们不触发字节测试但改变引擎行为。已在 ARCHITECTURE 中标注"动前必须过 replica 回归"。

## P0（矛盾/缺陷）→ 全部已修（v12.0.1）

1. **「现在」行讲引擎剧本、「计划」行讲跟单剧本**（"持有多头 runner｜Stop=T1" vs "损不动"，持仓有无也可互相矛盾）→ v12 模式下「现在」行改跟单叙事（持多单｜已兑50%·看T2｜损不动 / 空仓｜同向冷却…），着色跟随跟单方向；关闭 v12 时保留引擎叙事。
2. **10m 中继的旧计划事件推送漏 f12 闸门**（3m 路径有闸、中继路径没有：10m 图挂 alert 的用户会收到与"止损不动"打架的保本/结构叙事）→ 补齐同款闸门；测试断言两路闸门 `not f12Enable or f12EnginePushes` ×2。
3. **右缘 Stop/T1/T2 价签未随 v12 关闭**（引擎 effectiveStop 移保本后与跟单红线同屏两个止损价）→ 三处价签加 `not f12Enable`。

## P1（冗余/一致性/长度）→ 已修

4. 10m 中继历史标签无遮罩（违反双窗恒等式 + 标签洪水风险）→ 套用与 3m 相同的启动类/加仓/逆势三道遮罩。
5. 引擎计划事件形状与跟单标签双流并行 → 新增 `showEngineEvents`（默认关，关闭 v12 时恒显示）。
6. 死输入 showLevels 删除；「计划」行瘦身（日锚/已走% 迁「背景」行、冷却态迁「现在」行）；非 3m/10m 主机显示"跟单状态请看 3m/10m 图"；「下一步」行触发价去重。
7. 跟单持仓期隐藏最新信号卡（五层冗余降一层；空仓时保留作引擎雷达）。

## P2（打磨）→ 已修

死 alias（nextBuy/SellTrigger）与撒谎注释、F12State.openCt 死字段、10m 恒隐藏的卡片 tooltip 死文案、"five→three objects" 注释、输入组 06/07 物理交错归位、文件头补 12.0 契约段。

## 同批叠加（非审查项）

记分板**成本双轨**：新增 `f12CostPts` 输入（默认 0.5 点/往返，仅记分板显示用），F12State 增 totRNet，平仓推送与「计划」行同时显示毛 R 与费后 R；经状态中继镜像到 10m。**不改任何交易规则**（预登记连续性保持）。

## 明确不做（含理由）

颜色常量化（纯外观独立批次）、Saty 比例表三副本合并（行为区风险>收益）、订单模块 alert_message 品牌（语义冻结）、m12Wins 删除（快照对称）。

## 资源与合规复核

plot 50/64、线 24/30、标签 400（中继遮罩后大幅减洪）、security 8/40；CE10057 污染纪律合规（跟单状态写入作用域零 alert/label）。
